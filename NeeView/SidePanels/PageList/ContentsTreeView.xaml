<UserControl x:Class="NeeView.ContentsTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:ZeroToVisibleConverter x:Key="ZeroToVisibleConverter"/>
    </UserControl.Resources>

    <Grid x:Name="Root">
        <TreeView x:Name="ContentsTree"
                ItemsSource="{Binding Items}"
                LostFocus="ContentsTree_LostFocus"
                IsVisibleChanged="ContentsTree_IsVisibleChanged"
                SelectedItemChanged="ContentsTree_SelectedItemChanged"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                VirtualizingStackPanel.ScrollUnit="Item"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                FocusVisualStyle="{StaticResource NVFocusVisual}"
                >

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType= "local:IndexPageNode" ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource NVTreeViewItem}">
                    <Setter Property="IsExpanded" Value="True"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

    </Grid>
</UserControl>
